# Copy this file to `.env` (git-ignored) and fill in the keys you use.
# The run scripts auto-load .env via scripts/_common.sh.
#
#   OPENAI_API_KEY     — GPT-4o (base + constitution), text-embedding-3-small
#   ANTHROPIC_API_KEY  — Claude dynamic analyzer (vulnerable-code task)
#   TOGETHER_API_KEY   — open-source base models (Llama-3-8B, Qwen2.5)

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
TOGETHER_API_KEY=
